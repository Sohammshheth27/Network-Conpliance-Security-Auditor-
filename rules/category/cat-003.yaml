id: NCSA-CAT-003
title: Every rule must resolve to a concrete service
field: firewall.rules.service
operator: is_set
severity: medium
tier: category
rationale: >-
  A rule whose service cannot be resolved cannot be assessed for exposure.
frameworks:
  nist_800_53: ["AC-4"]
  iso_27001_2022: ["A.5.14", "A.8.22", "A.8.23"]
  # STIG/CIS ids intentionally omitted: they are per-platform and
  # belong in the vendor pack (plan 6.1). An invented id is worse
  # than none -- see the v1.4 6.4 caveat.
