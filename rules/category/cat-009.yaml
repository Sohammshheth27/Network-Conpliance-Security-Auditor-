id: NCSA-CAT-009
title: Egress traffic should be restricted
field: cloud.security_groups.egress
operator: is_set
severity: medium
tier: category
rationale: >-
  Unrestricted egress is the exfiltration path in most cloud incidents.
frameworks:
  nist_800_53: ["AC-4", "SC-7"]
  iso_27001_2022: ["A.5.14", "A.8.16", "A.8.20", "A.8.22", "A.8.23"]
  # STIG/CIS ids intentionally omitted: they are per-platform and
  # belong in the vendor pack (plan 6.1). An invented id is worse
  # than none -- see the v1.4 6.4 caveat.
