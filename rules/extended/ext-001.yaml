id: NCSA-EXT-001
title: SSH must use strong key exchange and ciphers
field: crypto.weak_ciphers
operator: max_count
expected: 0
severity: high
tier: extended
rationale: >-
  A strong protocol negotiated down to SHA-1 key exchange or a CBC cipher provides no meaningful protection.
frameworks:
  nist_800_53: ["SC-8.1", "SC-13"]
  iso_27001_2022: ["A.5.10", "A.5.14", "A.5.33", "A.8.20", "A.8.24", "A.8.26"]
  # STIG/CIS ids intentionally omitted: they are per-platform and
  # belong in the vendor pack (plan 6.1). An invented id is worse
  # than none -- see the v1.4 6.4 caveat.
