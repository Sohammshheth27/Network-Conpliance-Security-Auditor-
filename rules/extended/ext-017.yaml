id: NCSA-EXT-017
title: Command accounting must be enabled
field: logging.command_accounting
operator: equals
expected: true
severity: medium
tier: extended
rationale: >-
  Without command accounting there is no record of what an administrator did.
frameworks:
  nist_800_53: ["AU-2", "AU-12"]
  iso_27001_2022: ["A.8.15"]
  # STIG/CIS ids intentionally omitted: they are per-platform and
  # belong in the vendor pack (plan 6.1). An invented id is worse
  # than none -- see the v1.4 6.4 caveat.
