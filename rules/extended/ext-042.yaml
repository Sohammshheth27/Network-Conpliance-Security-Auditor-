id: NCSA-EXT-042
title: Interfaces should carry a description
field: interfaces.description
operator: is_set
severity: low
tier: extended
rationale: >-
  Undocumented interfaces are how forgotten paths survive audits.
frameworks:
  nist_800_53: ["CM-8"]
  iso_27001_2022: ["A.5.9", "A.8.9"]
  # STIG/CIS ids intentionally omitted: they are per-platform and
  # belong in the vendor pack (plan 6.1). An invented id is worse
  # than none -- see the v1.4 6.4 caveat.
