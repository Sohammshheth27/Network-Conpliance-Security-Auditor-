id: NCSA-EXT-037
title: Routing protocol authentication must be enabled
field: routing.ospf_auth
operator: equals
expected: true
severity: high
tier: extended
rationale: >-
  Unauthenticated routing lets an adjacent device redirect traffic.
frameworks:
  nist_800_53: ["SC-8", "IA-3"]
  iso_27001_2022: ["A.5.10", "A.5.14", "A.5.33", "A.8.20", "A.8.26"]
  # STIG/CIS ids intentionally omitted: they are per-platform and
  # belong in the vendor pack (plan 6.1). An invented id is worse
  # than none -- see the v1.4 6.4 caveat.
