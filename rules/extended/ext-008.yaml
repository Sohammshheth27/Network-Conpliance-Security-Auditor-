id: NCSA-EXT-008
title: HTTP management must redirect to HTTPS
field: management.http.redirect_https
operator: equals
expected: true
severity: medium
tier: extended
rationale: >-
  A redirect prevents an administrator from silently transiting cleartext.
frameworks:
  nist_800_53: ["SC-8.1"]
  iso_27001_2022: ["A.5.10", "A.5.14", "A.5.33", "A.8.20", "A.8.26"]
  # STIG/CIS ids intentionally omitted: they are per-platform and
  # belong in the vendor pack (plan 6.1). An invented id is worse
  # than none -- see the v1.4 6.4 caveat.
